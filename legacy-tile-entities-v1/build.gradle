archivesBaseName = "legacy-tile-entities-v1"
version = getSubprojectVersion(project, "0.6.3")

dependencies {
	compile project(path: ':legacy-api-base')
	compile project(path: ':legacy-registries-v0')
}
