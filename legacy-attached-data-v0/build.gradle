archivesBaseName = "legacy-attached-data-v0"
version = getSubprojectVersion(project, "0.6.3")

dependencies {
	compile project(path: ':legacy-api-base')
	testImplementation project(path: ':legacy-interaction-events-v0')
	testImplementation project(path: ':legacy-registries-v0')
}
